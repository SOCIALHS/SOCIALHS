<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper 태그의 namespace 속성 필수 작성  -->  
<mapper namespace="bulltein">
	
	<select id="BullteinList" resultType="BullteinBoardVO">
		SELECT * FROM B_BOARD ORDER BY B_IDX
	</select>
	
	<insert id="BullteinWrite" parameterType="com.bc.minseong.command.BullteinBoardVO">
		INSERT INTO B_BOARD (BB_IDX, ID, TITLE, CONTENT, GOOD, BAD, L_IDX, BBS_IDX)
					VALUES(BB_SEQ.NEXTVAL, test, #{title}, #{content}, 0, 0, 1, 301)
	</insert>
	
	<update id="update" parameterType="com.bc.minseong.command.BullteinBoardVO">
		UPDATE B_BOARD
		   SET BB_IDX = #{bb_idx},
		       ID = #{id},
		       TITLE = #{title},
		       CONTENT = #{content},
		       GOOD = #{good},
		       BAD = #{bad},
		       REGDATE = SYSDATE
		 WHERE BB_IDX = #{bb_idx}      
	</update>
	
</mapper>  






