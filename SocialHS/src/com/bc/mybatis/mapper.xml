<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper 태그의 namespace 속성 필수 작성  -->  
<mapper namespace="socialhs">
	
	<select id="hobbyList" resultType="com.bc.main.vo.BbsCodeVO">
		SELECT * FROM BBS_CODE WHERE BBS_IDX BETWEEN 300 AND 500 ORDER BY BBS_NAME
	</select>
	
	
	<select id="studyList" resultType="com.bc.main.vo.BbsCodeVO">
		SELECT * FROM BBS_CODE WHERE BBS_IDX BETWEEN 700 AND 999 ORDER BY BBS_NAME
	</select>
	
	
	<select id="msnAlm" resultType="com.bc.main.vo.MessengerVO" parameterType="String">
		SELECT * FROM MESSENGER WHERE RECV_ID = #{id} AND CHK != 0 ORDER BY MS_IDX DESC
	</select>
	
	
	<select id="memAlm" resultType="com.bc.main.vo.BoardVO" parameterType="String">
		SELECT * FROM B_BOARD WHERE ID = #{id} AND CHK != 0 ORDER BY BB_IDX DESC
	</select>
	
	<select id="comAlm" resultType="com.bc.main.vo.CommentVO" parameterType="String">
		SELECT B.TITLE, C.*  
		FROM BB_COMMENT C, B_BOARD B  
		WHERE B.BB_IDX = C.BB_IDX 
		AND B.ID = #{id}  
		AND C.CHK = 1  
		ORDER BY BBC_IDX DESC 
	</select>
	
	
</mapper>  






